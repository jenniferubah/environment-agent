apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - nats.yaml
  - rbac.yaml
  - environment-agent.yaml

# Image tag is set at deploy time by deploy/scripts/k8s-deploy.sh.
